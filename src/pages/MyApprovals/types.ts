export type Department = {
  id: string,
  display: string
}

export type User = {
  id: string,
  name: string,
  type: string,
  start: number,
  end: number,
  duration: number,
  reason: string,
  comments: string,
  thumbnail: string
}